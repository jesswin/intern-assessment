class MyList {
  var heading;
  var subHeading;
  var dpUrl;
  MyList(this.heading, this.subHeading, [this.dpUrl]);
}

class Things {
  var emoji;
  var title;
  Things(this.title, [this.emoji]);
}

class People {
  var title;
  var subTitle;
  var link;
  var dpUrl;
  People(this.title, this.subTitle, this.link, this.dpUrl);
}
